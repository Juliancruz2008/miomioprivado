# Métodos de pago

`MetodosPagoPanel.tsx` gestiona el CRUD del usuario autenticado: lista, crea, edita,
activa o desactiva y elimina sus métodos de pago. Consume los endpoints protegidos
de `/pagos` mediante `src/api/metodosPago.api.ts`.
